// 우분투 노트북에서 RealSense D435f를 쓰기 위한 설치 단계

1. 먼저 카메라를 ROS보다 앞에서 확인합니다.
  순서는 librealsense 설치 
  → realsense-viewer로 카메라 확인 
  → ROS wrapper 설치 
  → ROS 토픽 확인이 가장 안정적입니다. 
    최근 librealsense는 Ubuntu 22.04를 지원하고, 
    ROS wrapper는 Humble에서 Debian 패키지 설치와 소스 설치 둘 다 안내하고 있습니다. 
    공식 wrapper 문서에는 Humble에서 ros-humble-realsense2-* 설치 예시가 있고, 
    최근 librealsense 릴리스 문서에도 Ubuntu 22.04 지원이 명시되어 있습니다

2. 먼저 기본 패키지와 RealSense SDK를 설치합니다.
  터미널에 아래 순서대로 입력(이 의존성들은 librealsense 공식 설치 문서의 기본 준비 항목)
sudo apt update
sudo apt upgrade -y
sudo apt install -y \libssl-dev libusb-1.0-0-dev libudev-dev pkg-config libgtk-3-dev \git cmake build-essential libglfw3-dev libgl1-mesa-dev libglu1-mesa-dev

3. RealSense 저장소를 추가하고 SDK와 Viewer를 설치합니다.
  우분투 PC 환경에서는 공식 Debian 저장소를 쓰는 방법이 가장 간단합니다. 
  실제 GitHub 이슈에서도 Ubuntu 22.04 PC에서 librealsense2-dkms, librealsense2-utils, librealsense2-dev 패키지 흐름을 사용하고 있고, 
  realsense-viewer는 librealsense2-utils에 포함됩니다.
  아래 그대로 진행

sudo mkdir -p /etc/apt/keyrings
curl -sSf https://librealsense.intel.com/Debian/librealsense.pgp | \ 
sudo gpg --dearmor -o /etc/apt/keyrings/librealsense.pgp
    
echo "deb [signed-by=/etc/apt/keyrings/librealsense.pgp] \https://librealsense.intel.com/Debian/apt-repo $(lsb_release -cs) main" | \
sudo tee /etc/apt/sources.list.d/librealsense.list
    
sudo apt update
sudo apt install -y librealsense2-dkms librealsense2-utils librealsense2-dev

  설치가 끝나면 Viewer가 있는지 확인
realsense-viewer

4. 카메라를 먼저 Viewer에서 확인
  카메라를 USB 3 포트에 연결한 뒤 
  realsense-viewer를 실행했을 때 컬러와 깊이 화면이 뜨면 SDK 단계는 통과입니다. 
  공식 이슈들에서도 “viewer에서 인식되는지”가 가장 기본 확인 단계로 쓰이고 있습니다

  추가 확인용으로는 아래도 좋습니다.
rs-enumerate-devices

5. 이제 ROS wrapper를 설치합니다.
  공식 realsense-ros README에는 Humble에서 Debian 패키지 설치로 
  sudo apt install ros-humble-realsense2-*를 안내하고 있습니다.

  아래를 입력하세요.
sudo apt update
sudo apt install -y ros-humble-realsense2-*

6. ROS에서 카메라를 실행합니다.
  새 터미널에서 아래처럼 실행하세요.
  
source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py

  point cloud까지 보고 싶으면 아래와 같이 실행하면 됩니다.
source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py enable_pointcloud:=true

 >>>  rs_launch.py는 공식 wrapper 저장소에 있는 기본 launch 파일

7. ROS 토픽이 뜨는지 확인합니다.
  카메라 런치 후 새 터미널에서 확인하세요.
source /opt/ros/humble/setup.bash
ros2 topic list | grep camera
>>> 보통 
/camera/color/image_raw, /camera/depth/image_rect_raw, /camera/depth/color/points 같은 토픽이 보이면 정상입니다. 
wrapper가 카메라 스트림과 point cloud를 ROS 토픽으로 내보내기 때문입니다


=============

1. 잘못 저장된 RealSense 저장소 파일 삭제

sudo rm /etc/apt/sources.list.d/librealsense.list

2. https 관련 패키지 설치
이 단계는 혹시 모를 문제를 막기 위한 준비입니다.

sudo apt update
sudo apt install -y apt-transport-https ca-certificates curl gnupg

3. RealSense 키 파일 다시 준비
이미 있어도 다시 해도 괜찮습니다.

sudo mkdir -p /etc/apt/keyrings
curl -sSf https://librealsense.intel.com/Debian/librealsense.pgp | sudo gpg --dearmor -o /etc/apt/keyrings/librealsense.pgp

여기서 “파일이 이미 있다, 덮어쓸까”가 나오면 y 입력하면 됩니다.

4. RealSense 저장소를 다시 정확하게 등록
여기서 가장 중요합니다.
반드시 https:// 앞에 \를 넣지 말아야 합니다.

echo "deb [signed-by=/etc/apt/keyrings/librealsense.pgp] https://librealsense.intel.com/Debian/apt-repo $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/librealsense.list

5. 저장소가 제대로 들어갔는지 확인

cat /etc/apt/sources.list.d/librealsense.list

정상이라면 결과가 이렇게 보여야 합니다.

deb [signed-by=/etc/apt/keyrings/librealsense.pgp] https://librealsense.intel.com/Debian/apt-repo jammy main

여기서 \https가 보이면 다시 잘못된 것이고, https://로 보여야 정상입니다.

6. 패키지 목록 갱신

sudo apt update

이 단계에서 빨간 오류가 없이 끝나야 합니다.

7. RealSense 패키지 설치

sudo apt install -y librealsense2-dkms librealsense2-utils librealsense2-dev

8.설치 확인
설치가 끝나면 카메라를 연결하고 아래를 실행합니다.

realsense-viewer

9. 만약 realsense-viewer가 안 열리면 장치 인식 확인

rs-enumerate-devices

=====================

1. RealSense ROS 패키지 설치
터미널에서 아래를 입력하세요.

sudo apt update
sudo apt install -y ros-humble-realsense2-*

2. ROS에서 카메라 실행
설치가 끝나면 새 터미널을 열고 입력합니다.

source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py

3. 카메라 토픽 확인
또 다른 새 터미널을 열고 입력합니다.

source /opt/ros/humble/setup.bash
ros2 topic list | grep camera

정상이라면 이런 토픽들이 보일 가능성이 큽니다.

/camera/color/image_raw
/camera/depth/image_rect_raw
/camera/depth/color/points

4. 토픽 내용 확인
예를 들어 컬러 영상 토픽이 실제로 나오는지 확인하려면

source /opt/ros/humble/setup.bash
ros2 topic hz /camera/color/image_raw

깊이 토픽이 나오는지 보려면

source /opt/ros/humble/setup.bash
ros2 topic hz /camera/depth/image_rect_raw

====================================

ROS 설치 완료
카메라 SDK 설치 완료
realsense-viewer 확인 완료
ROS에서 카메라 노드 실행 완료
카메라 토픽 생성 확인 완료

1단계. ROS에서 영상 직접 보기
  
  이 단계 목적은 아주 단순합니다.
  “카메라 영상이 ROS 안에서 실제로 들어오고 있구나”를 눈으로 확인하는 것입니다.
  
  먼저 카메라 노드를 실행하는 터미널이 하나 필요합니다.
  
  터미널 1에서 실행
source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py

  이 터미널은 켜둔 상태로 둡니다.
  그 다음 새 터미널을 엽니다.
  터미널 2에서 실행

source /opt/ros/humble/setup.bash
rqt_image_view

  창이 열리면 상단 드롭다운에서 아래 토픽을 선택합니다.
/camera/camera/color/image_raw
  
  그러면 컬러 영상이 보여야 정상입니다.
  깊이 영상도 보고 싶으면 드롭다운에서 이것도 선택해 봅니다.

/camera/camera/depth/image_rect_raw

여기까지 되면 1단계 완료입니다.

카메라 영상이 ROS 토픽으로 들어오고 있다
즉 구조는 이렇게 됩니다.

카메라
→ ROS 노드
→ image_raw 토픽
→ rqt_image_view가 그 토픽을 화면으로 보여줌


-----

2단계. 영상 데이터 활용해서 좌표 계산

이 단계 목적은 영상 자체를 보는 게 아니라, 영상 속 물체 위치를 숫자로 바꾸는 것입니다.
로봇팔 제어를 하려면 결국 숫자 좌표가 필요하거든요.

여기서는 처음부터 복잡하게 가지 말고 가장 쉬운 방법으로 시작하는 게 좋습니다.

추천 시작 방식은 색상 물체 하나를 정해서 그 중심 좌표를 찾는 것입니다.
예를 들면 빨간 물체 하나를 놓고, 카메라 화면에서 그 물체 중심의 x, y 값을 찾는 방식입니다.

흐름은 이렇게 됩니다.

컬러 영상 받기
→ OpenCV로 색상 추출
→ 물체 중심 좌표 계산
→ 좌표를 ROS 토픽으로 publish

이 단계에서 필요한 노드는 새로 하나 만들면 됩니다.

예를 들면 이름은 이런 식으로 잡을 수 있어요.

color_tracker_node

이 노드는
카메라 영상 subscribe
→ 물체 좌표 계산
→ /target_xy 같은 토픽 publish

이렇게 동작합니다.

즉 이 단계가 되면 구조가 이렇게 바뀝니다.

카메라
→ /camera/camera/color/image_raw
→ color_tracker_node
→ /target_xy

여기서 /target_xy는 나중에 로봇팔 제어 입력값이 됩니다.

------------

3단계. 아두이노와 연결

이 단계 목적은 ROS가 계산한 좌표나 각도 명령을 아두이노로 보내는 것입니다.

구조는 이렇게 됩니다.

ROS 노트북
→ USB 시리얼
→ Arduino
→ PCA9685
→ 서보 6개

여기서 ROS는 직접 서보를 움직이지 않습니다.
ROS는 명령만 보내고, 실제 PWM 제어는 아두이노가 합니다.

처음에는 가장 단순하게 가면 됩니다.

ROS에서 문자열 또는 숫자 1개 전송
→ 아두이노가 받아서 시리얼 모니터에 출력

이게 먼저입니다.

즉 처음부터 6개 서보 다 제어하지 말고,
“ROS에서 값 보내기 → 아두이노가 받기”
이 통신부터 먼저 확인합니다.

예를 들면 ROS가 이런 문자열을 보냅니다.
Bash 90 

그러면 아두이노가 그 값을 읽고
servo angle = 90  
이렇게 해석하는 구조입니다.

그 다음 단계에서 이 값을 PCA9685 채널 0에 넣어 서보 하나를 움직입니다.
그 다음 6개로 확장하면 됩니다.

4단계. 로봇팔 제어

이 단계가 최종입니다.

이제 구조가 완성됩니다.

카메라
→ 물체 위치 계산
→ 목표 좌표 생성
→ ROS 제어 노드
→ 아두이노로 각도 전송
→ PCA9685
→ 서보 6개
→ 로봇팔 움직임

여기서는 보통 중간에 제어 노드가 하나 더 들어갑니다.

예를 들면

tracker_node
→ 물체 좌표 publish

arm_control_node
→ 좌표를 관절 각도로 변환

serial_sender_node
→ 아두이노로 각도 전송

즉 ROS 안에서도 기능을 나눠서 처리합니다.

지금 수미가 당장 해야 할 것은 1단계입니다.
오늘 기준으로는 여기까지만 딱 깔끔하게 하면 좋습니다.

실행 순서만 다시 아주 짧게 적으면 이렇습니다.

첫 번째 터미널
source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py

두 번째 터미널
source /opt/ros/humble/setup.bash
rqt_image_view

그리고 드롭다운에서
/camera/camera/color/image_raw

선택

이 화면이 잘 뜨면, 다음 단계로 바로
“영상에서 색상 물체 좌표 뽑기”
코드로 넘어가면 됩니다.
